# local .env style (KEY=VALUE). NOTE: repo root .env currently holds ONLY the bare gsk_ key —
# backend also accepts that bare-key format. For Docker, use this format:
GROQ_API_KEY=gsk_paste_yours_here
GROQ_MODEL=openai/gpt-oss-120b
MONGO_URI=mongodb://localhost:27017
MONGO_DB=chatpdfs
RAG_TOP_K=5
